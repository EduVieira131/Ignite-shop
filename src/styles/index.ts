import { createStitches } from '@stitches/react'

export const {
  config,
  styled,
  globalCss,
  keyframes,
  css,
  getCssText,
  theme,
  createTheme
} = createStitches({
  theme: {
    colors: {
      primary100: '#f5f8fa'
    }
  }
})
